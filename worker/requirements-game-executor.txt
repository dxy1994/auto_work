# ═══════════════════════════════════════════════
# 游戏执行端独立依赖 — 不包含浏览器监控组件
# 安装: pip install -r requirements-game-executor.txt
# ═══════════════════════════════════════════════
-r requirements-common.txt
# pyserial>=3.5                # ESP32C3 串口通信（固件就绪后启用）
opencv-contrib-python==4.10.0.84
Pillow>=10,<13
numpy>=1.26,<2.3
pywin32>=306; sys_platform == "win32"
paddlepaddle==3.3.1
paddleocr==3.7.0
