<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.auto.mapper.GameItemOrderDetailMapper">

    <select id="sumSubtotalByOrderId" resultType="java.math.BigDecimal">
        select coalesce(sum(subtotal), 0) from game_item_order_details where order_id = #{orderId}
    </select>

</mapper>
